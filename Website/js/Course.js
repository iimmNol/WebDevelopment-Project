        // JavaScript to handle redirection on toggle switch
document.getElementById('toggleSwitch').addEventListener('change', function() {
  if (!this.checked) {
    this.checked = true;
    // Redirect to a PHP file or a specific page
    window.location.href = "Introduction.php"; // Change to the desired file
  }
});

// JavaScript to handle redirection on toggle switch
document.getElementById('toggleSwitch2').addEventListener('change', function() {
  if (!this.checked) {
    this.checked = true;
    // Redirect to a PHP file or a specific page
    window.location.href = "BasicSyntax.php"; // Change to the desired file
  }
});


// JavaScript to handle redirection on toggle switch
document.getElementById('toggleSwitch3').addEventListener('change', function() {
  if (!this.checked) {
    this.checked = true;
    // Redirect to a PHP file or a specific page
    window.location.href = "Condition.php"; // Change to the desired file
  }
});

// JavaScript to handle redirection on toggle switch
document.getElementById('toggleSwitch4').addEventListener('change', function() {
if (!this.checked) {
  this.checked = true;
  // Redirect to a PHP file or a specific page
  window.location.href = "Arrays.php"; // Change to the desired file
}
});

// JavaScript to handle redirection on toggle switch
document.getElementById('toggleSwitch5').addEventListener('change', function() {
if (!this.checked) {
this.checked = true;
// Redirect to a PHP file or a specific page
window.location.href = "Classes.php"; // Change to the desired file
}
});

// JavaScript to handle redirection on toggle switch
document.getElementById('toggleSwitch6').addEventListener('change', function() {
if (!this.checked) {
this.checked = true;
// Redirect to a PHP file or a specific page
window.location.href = "MySql.php"; // Change to the desired file
}
});







